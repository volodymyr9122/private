import React, {Component} from 'react'

const MoreButton = ({onClick}) => {
  return(
    <div>
      <button onClick = {onClick}>See more posts... </button>
    </div>
  )
}
export default MoreButton
